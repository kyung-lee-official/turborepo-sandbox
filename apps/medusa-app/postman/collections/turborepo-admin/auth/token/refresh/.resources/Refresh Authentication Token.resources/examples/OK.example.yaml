$kind: http-example
request:
  url: "{{baseUrl}}/auth/token/refresh"
  method: POST
  headers:
    Accept: application/json
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "token": "string"
      }
order: 1000
