$kind: http-example
request:
  url: "{{baseUrl}}/admin/returns/:id/receive-items/:action_id?fields=string&id=s\
    tring&order_id=string&status=string&$eq=string&$ne=string&$in=string,string\
    &$nin=string,string&$not=string&$gt=string&$gte=string&$lt=string&$lte=stri\
    ng&$like=string&$re=string&$ilike=string&$fulltext=string&$overlap=string,s\
    tring&$contains=string,string&$contained=string,string&$exists=false&limit=\
    3050.1607324519186&offset=3157.9117089204024&order=string&with_deleted=true"
  method: DELETE
  headers:
    - key: Accept
      value: application/json
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    - key: fields
      value: string
      description: Comma-separated fields that should be included in the returned
        data. if a field is prefixed with `+` it will be added to the default
        fields, using `-` will remove it from the default fields. without prefix
        it will replace the entire default fields.
    - key: id
      value: string
    - key: order_id
      value: string
    - key: status
      value: string
    - key: $eq
      value: string
      description: Filter by a return's update date.
    - key: $ne
      value: string
      description: Filter by a return's update date.
    - key: $in
      value: string,string
      description: Filter by a return's update date.
    - key: $nin
      value: string,string
      description: Filter by a return's update date.
    - key: $not
      value: string
      description: Filter by a return's update date.
    - key: $gt
      value: string
      description: Filter by a return's update date.
    - key: $gte
      value: string
      description: Filter by a return's update date.
    - key: $lt
      value: string
      description: Filter by a return's update date.
    - key: $lte
      value: string
      description: Filter by a return's update date.
    - key: $like
      value: string
      description: Filter by a return's update date.
    - key: $re
      value: string
      description: Filter by a return's update date.
    - key: $ilike
      value: string
      description: Filter by a return's update date.
    - key: $fulltext
      value: string
      description: Filter by a return's update date.
    - key: $overlap
      value: string,string
      description: Filter by a return's update date.
    - key: $contains
      value: string,string
      description: Filter by a return's update date.
    - key: $contained
      value: string,string
      description: Filter by a return's update date.
    - key: $exists
      value: "false"
      description: Filter by a return's update date.
    - key: limit
      value: "3050.1607324519186"
      description: Limit the number of items returned in the list.
    - key: offset
      value: "3157.9117089204024"
      description: The number of items to skip when retrieving a list.
    - key: order
      value: string
      description: The field to sort the data by. By default, the sort order is
        ascending. To change the order to descending, prefix the field name with
        `-`.
    - key: with_deleted
      value: "true"
      description: The return's with deleted.
  pathVariables:
    - key: id
      value: string
      description: The return's ID.
    - key: action_id
      value: string
      description: The ID of the received item's `RECEIVE_RETURN_ITEM` action.
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "message": "Discount must be set to dynamic",
        "type": "not_allowed"
      }
order: 2000
