$kind: http-example
request:
  url: "{{baseUrl}}/admin/claims/:id/claim-items?fields=string&id=string&status=s\
    tring&$eq=string&$ne=string&$in=string,string&$nin=string,string&$not=strin\
    g&$gt=string&$gte=string&$lt=string&$lte=string&$like=string&$re=string&$il\
    ike=string&$fulltext=string&$overlap=string,string&$contains=string,string&\
    $contained=string,string&$exists=false"
  method: POST
  headers:
    - key: Content-Type
      value: application/json
    - key: Accept
      value: application/json
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    - key: fields
      value: string
      description: Comma-separated fields that should be included in the returned
        data. if a field is prefixed with `+` it will be added to the default
        fields, using `-` will remove it from the default fields. without prefix
        it will replace the entire default fields.
    - key: id
      value: string
    - key: status
      value: string
    - key: $eq
      value: string
      description: Filter by a claim's creation date.
    - key: $ne
      value: string
      description: Filter by a claim's creation date.
    - key: $in
      value: string,string
      description: Filter by a claim's creation date.
    - key: $nin
      value: string,string
      description: Filter by a claim's creation date.
    - key: $not
      value: string
      description: Filter by a claim's creation date.
    - key: $gt
      value: string
      description: Filter by a claim's creation date.
    - key: $gte
      value: string
      description: Filter by a claim's creation date.
    - key: $lt
      value: string
      description: Filter by a claim's creation date.
    - key: $lte
      value: string
      description: Filter by a claim's creation date.
    - key: $like
      value: string
      description: Filter by a claim's creation date.
    - key: $re
      value: string
      description: Filter by a claim's creation date.
    - key: $ilike
      value: string
      description: Filter by a claim's creation date.
    - key: $fulltext
      value: string
      description: Filter by a claim's creation date.
    - key: $overlap
      value: string,string
      description: Filter by a claim's creation date.
    - key: $contains
      value: string,string
      description: Filter by a claim's creation date.
    - key: $contained
      value: string,string
      description: Filter by a claim's creation date.
    - key: $exists
      value: "false"
      description: Filter by a claim's creation date.
  pathVariables:
    - key: id
      value: string
      description: The claim's ID.
  body:
    type: json
    content: |-
      {
        "items": [
          {
            "id": "string",
            "quantity": 4717.10314549415,
            "reason": "other",
            "description": "string",
            "internal_note": "string"
          },
          {
            "id": "string",
            "quantity": 6180.042482439758,
            "reason": "production_failure",
            "description": "string",
            "internal_note": "string"
          }
        ]
      }
response:
  statusCode: 422
  statusText: Unprocessable Entity (WebDAV) (RFC 4918)
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "code": "invalid_request_error",
        "message": "Discount with code TEST already exists.",
        "type": "duplicate_error"
      }
order: 6000
