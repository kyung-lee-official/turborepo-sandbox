$kind: http-request
description: Confirm a claim request, applying its changes on the associated order.
url: "{{baseUrl}}/admin/claims/:id/request?fields=string&id=string&status=strin\
  g&$eq=string&$ne=string&$in=string,string&$nin=string,string&$not=string&$gt=\
  string&$gte=string&$lt=string&$lte=string&$like=string&$re=string&$ilike=stri\
  ng&$fulltext=string&$overlap=string,string&$contains=string,string&$contained\
  =string,string&$exists=false&$eq=string&$ne=string&$in=string,string&$nin=str\
  ing,string&$not=string&$gt=string&$gte=string&$lt=string&$lte=string&$like=st\
  ring&$re=string&$ilike=string&$fulltext=string&$overlap=string,string&$contai\
  ns=string,string&$contained=string,string&$exists=true&$eq=string&$ne=string&\
  $in=string,string&$nin=string,string&$not=string&$gt=string&$gte=string&$lt=s\
  tring&$lte=string&$like=string&$re=string&$ilike=string&$fulltext=string&$ove\
  rlap=string,string&$contains=string,string&$contained=string,string&$exists=f\
  alse"
method: POST
headers:
  Accept: application/json
queryParams:
  - key: fields
    value: string
    description: Comma-separated fields that should be included in the returned
      data. if a field is prefixed with `+` it will be added to the default
      fields, using `-` will remove it from the default fields. without prefix
      it will replace the entire default fields.
  - key: id
    value: string
  - key: status
    value: string
  - key: $eq
    value: string
    description: Filter by a claim's creation date.
  - key: $ne
    value: string
    description: Filter by a claim's creation date.
  - key: $in
    value: string,string
    description: Filter by a claim's creation date.
  - key: $nin
    value: string,string
    description: Filter by a claim's creation date.
  - key: $not
    value: string
    description: Filter by a claim's creation date.
  - key: $gt
    value: string
    description: Filter by a claim's creation date.
  - key: $gte
    value: string
    description: Filter by a claim's creation date.
  - key: $lt
    value: string
    description: Filter by a claim's creation date.
  - key: $lte
    value: string
    description: Filter by a claim's creation date.
  - key: $like
    value: string
    description: Filter by a claim's creation date.
  - key: $re
    value: string
    description: Filter by a claim's creation date.
  - key: $ilike
    value: string
    description: Filter by a claim's creation date.
  - key: $fulltext
    value: string
    description: Filter by a claim's creation date.
  - key: $overlap
    value: string,string
    description: Filter by a claim's creation date.
  - key: $contains
    value: string,string
    description: Filter by a claim's creation date.
  - key: $contained
    value: string,string
    description: Filter by a claim's creation date.
  - key: $exists
    value: "false"
    description: Filter by a claim's creation date.
  - key: $eq
    value: string
    description: Filter by a claim's update date.
  - key: $ne
    value: string
    description: Filter by a claim's update date.
  - key: $in
    value: string,string
    description: Filter by a claim's update date.
  - key: $nin
    value: string,string
    description: Filter by a claim's update date.
  - key: $not
    value: string
    description: Filter by a claim's update date.
  - key: $gt
    value: string
    description: Filter by a claim's update date.
  - key: $gte
    value: string
    description: Filter by a claim's update date.
  - key: $lt
    value: string
    description: Filter by a claim's update date.
  - key: $lte
    value: string
    description: Filter by a claim's update date.
  - key: $like
    value: string
    description: Filter by a claim's update date.
  - key: $re
    value: string
    description: Filter by a claim's update date.
  - key: $ilike
    value: string
    description: Filter by a claim's update date.
  - key: $fulltext
    value: string
    description: Filter by a claim's update date.
  - key: $overlap
    value: string,string
    description: Filter by a claim's update date.
  - key: $contains
    value: string,string
    description: Filter by a claim's update date.
  - key: $contained
    value: string,string
    description: Filter by a claim's update date.
  - key: $exists
    value: "true"
    description: Filter by a claim's update date.
  - key: $eq
    value: string
    description: Filter by a claim's deletion date.
  - key: $ne
    value: string
    description: Filter by a claim's deletion date.
  - key: $in
    value: string,string
    description: Filter by a claim's deletion date.
  - key: $nin
    value: string,string
    description: Filter by a claim's deletion date.
  - key: $not
    value: string
    description: Filter by a claim's deletion date.
  - key: $gt
    value: string
    description: Filter by a claim's deletion date.
  - key: $gte
    value: string
    description: Filter by a claim's deletion date.
  - key: $lt
    value: string
    description: Filter by a claim's deletion date.
  - key: $lte
    value: string
    description: Filter by a claim's deletion date.
  - key: $like
    value: string
    description: Filter by a claim's deletion date.
  - key: $re
    value: string
    description: Filter by a claim's deletion date.
  - key: $ilike
    value: string
    description: Filter by a claim's deletion date.
  - key: $fulltext
    value: string
    description: Filter by a claim's deletion date.
  - key: $overlap
    value: string,string
    description: Filter by a claim's deletion date.
  - key: $contains
    value: string,string
    description: Filter by a claim's deletion date.
  - key: $contained
    value: string,string
    description: Filter by a claim's deletion date.
  - key: $exists
    value: "false"
    description: Filter by a claim's deletion date.
pathVariables:
  - key: id
    value: string
    description: The claim's ID.
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Confirm a Claim Request.resources/examples
order: 1000
