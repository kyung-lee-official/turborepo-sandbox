$kind: http-request
description: Retrieve a list of draft orders. The draft orders can be filtered
  by fields such as `id`. The draft orders can also be sorted or paginated.
url: "{{baseUrl}}/admin/draft-orders?fields=string&offset=3157.9117089204024&li\
  mit=3050.1607324519186&order=string&id=string&status=string&sales_channel_id=\
  string&sales_channel_id=string&region_id=string&q=string&$eq=string&$ne=strin\
  g&$in=string,string&$nin=string,string&$not=string&$gt=string&$gte=string&$lt\
  =string&$lte=string&$like=string&$re=string&$ilike=string&$fulltext=string&$o\
  verlap=string,string&$contains=string,string&$contained=string,string&$exists\
  =true&$eq=string&$ne=string&$in=string,string&$nin=string,string&$not=string&\
  $gt=string&$gte=string&$lt=string&$lte=string&$like=string&$re=string&$ilike=\
  string&$fulltext=string&$overlap=string,string&$contains=string,string&$conta\
  ined=string,string&$exists=false&customer_id=string&with_deleted=true"
method: GET
headers:
  Accept: application/json
queryParams:
  - key: fields
    value: string
    description: Comma-separated fields that should be included in the returned
      data. if a field is prefixed with `+` it will be added to the default
      fields, using `-` will remove it from the default fields. without prefix
      it will replace the entire default fields.
  - key: offset
    value: "3157.9117089204024"
    description: The number of items to skip when retrieving a list.
  - key: limit
    value: "3050.1607324519186"
    description: Limit the number of items returned in the list.
  - key: order
    value: string
    description: The field to sort the data by. By default, the sort order is
      ascending. To change the order to descending, prefix the field name with
      `-`.
  - key: id
    value: string
  - key: status
    value: string
  - key: sales_channel_id
    value: string
    description: Filter by the associated sales channels to retrieve its draft orders.
  - key: sales_channel_id
    value: string
    description: Filter by the associated sales channels to retrieve its draft orders.
  - key: region_id
    value: string
    description: Filter by region IDs to retrieve their associated draft orders.
  - key: q
    value: string
    description: Search term to filter the order's searchable properties.
  - key: $eq
    value: string
    description: Filter by the draft order's creation date.
  - key: $ne
    value: string
    description: Filter by the draft order's creation date.
  - key: $in
    value: string,string
    description: Filter by the draft order's creation date.
  - key: $nin
    value: string,string
    description: Filter by the draft order's creation date.
  - key: $not
    value: string
    description: Filter by the draft order's creation date.
  - key: $gt
    value: string
    description: Filter by the draft order's creation date.
  - key: $gte
    value: string
    description: Filter by the draft order's creation date.
  - key: $lt
    value: string
    description: Filter by the draft order's creation date.
  - key: $lte
    value: string
    description: Filter by the draft order's creation date.
  - key: $like
    value: string
    description: Filter by the draft order's creation date.
  - key: $re
    value: string
    description: Filter by the draft order's creation date.
  - key: $ilike
    value: string
    description: Filter by the draft order's creation date.
  - key: $fulltext
    value: string
    description: Filter by the draft order's creation date.
  - key: $overlap
    value: string,string
    description: Filter by the draft order's creation date.
  - key: $contains
    value: string,string
    description: Filter by the draft order's creation date.
  - key: $contained
    value: string,string
    description: Filter by the draft order's creation date.
  - key: $exists
    value: "true"
    description: Filter by the draft order's creation date.
  - key: $eq
    value: string
    description: Filter by the draft order's update date.
  - key: $ne
    value: string
    description: Filter by the draft order's update date.
  - key: $in
    value: string,string
    description: Filter by the draft order's update date.
  - key: $nin
    value: string,string
    description: Filter by the draft order's update date.
  - key: $not
    value: string
    description: Filter by the draft order's update date.
  - key: $gt
    value: string
    description: Filter by the draft order's update date.
  - key: $gte
    value: string
    description: Filter by the draft order's update date.
  - key: $lt
    value: string
    description: Filter by the draft order's update date.
  - key: $lte
    value: string
    description: Filter by the draft order's update date.
  - key: $like
    value: string
    description: Filter by the draft order's update date.
  - key: $re
    value: string
    description: Filter by the draft order's update date.
  - key: $ilike
    value: string
    description: Filter by the draft order's update date.
  - key: $fulltext
    value: string
    description: Filter by the draft order's update date.
  - key: $overlap
    value: string,string
    description: Filter by the draft order's update date.
  - key: $contains
    value: string,string
    description: Filter by the draft order's update date.
  - key: $contained
    value: string,string
    description: Filter by the draft order's update date.
  - key: $exists
    value: "false"
    description: Filter by the draft order's update date.
  - key: customer_id
    value: string
  - key: with_deleted
    value: "true"
    description: Whether to include deleted records in the result.
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/List Draft Orders.resources/examples
order: 2000
