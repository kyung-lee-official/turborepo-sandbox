$kind: http-request
description: Add promotions to a draft order edit.
url: "{{baseUrl}}/admin/draft-orders/:id/edit/promotions"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
pathVariables:
  - key: id
    value: string
    description: The draft order's ID.
body:
  type: json
  content: |-
    {
      "promo_codes": [
        "string",
        "string"
      ]
    }
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Add Promotions to Draft Order Edit.resources/examples
order: 1000
