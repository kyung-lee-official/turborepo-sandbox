$kind: http-example
request:
  url: "{{baseUrl}}/admin/products?fields=string&offset=4756.114149372268&limit=7\
    711.6466097095035&order=string&price_list_id=string&q=string&status=string&\
    sales_channel_id=string&title=string&handle=string&id=string&is_giftcard=tr\
    ue&value=string,string&type_id=string&category_id=string&$eq=string&$ne=str\
    ing&$in=string,string&$nin=string,string&$not=string&$gt=string&$gte=string\
    &$lt=string&$lte=string&$like=string&$re=string&$ilike=string&$fulltext=str\
    ing&$overlap=string,string&$contains=string,string&$contained=string,string\
    &$exists=false&collection_id=string&manage_inventory=true&allow_backorder=f\
    alse&created_at=[object Object]&updated_at=[object
    Object]&deleted_at=[object
    Object]&ean=string&upc=string&barcode=string&with_deleted=true"
  method: GET
  headers:
    - key: Accept
      value: application/json
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    - key: fields
      value: string
      description: Comma-separated fields that should be included in the returned
        data. if a field is prefixed with `+` it will be added to the default
        fields, using `-` will remove it from the default fields. without prefix
        it will replace the entire default fields.
    - key: offset
      value: "4756.114149372268"
      description: The number of items to skip when retrieving a list.
    - key: limit
      value: "7711.6466097095035"
      description: Limit the number of items returned in the list.
    - key: order
      value: string
      description: The field to sort the data by. By default, the sort order is
        ascending. To change the order to descending, prefix the field name with
        `-`.
    - key: price_list_id
      value: string
    - key: q
      value: string
      description: Search term to filter a product's searchable properties.
    - key: status
      value: string
    - key: sales_channel_id
      value: string
    - key: title
      value: string
    - key: handle
      value: string
    - key: id
      value: string
    - key: is_giftcard
      value: "true"
      description: Filter by whether a product is a gift card.
    - key: value
      value: string,string
      description: Filter by associated tags to retrieve their products.
    - key: type_id
      value: string
    - key: category_id
      value: string
    - key: $eq
      value: string
      description: Filter by a product's creation date.
    - key: $ne
      value: string
      description: Filter by a product's creation date.
    - key: $in
      value: string,string
      description: Filter by a product's creation date.
    - key: $nin
      value: string,string
      description: Filter by a product's creation date.
    - key: $not
      value: string
      description: Filter by a product's creation date.
    - key: $gt
      value: string
      description: Filter by a product's creation date.
    - key: $gte
      value: string
      description: Filter by a product's creation date.
    - key: $lt
      value: string
      description: Filter by a product's creation date.
    - key: $lte
      value: string
      description: Filter by a product's creation date.
    - key: $like
      value: string
      description: Filter by a product's creation date.
    - key: $re
      value: string
      description: Filter by a product's creation date.
    - key: $ilike
      value: string
      description: Filter by a product's creation date.
    - key: $fulltext
      value: string
      description: Filter by a product's creation date.
    - key: $overlap
      value: string,string
      description: Filter by a product's creation date.
    - key: $contains
      value: string,string
      description: Filter by a product's creation date.
    - key: $contained
      value: string,string
      description: Filter by a product's creation date.
    - key: $exists
      value: "false"
      description: Filter by a product's creation date.
    - key: collection_id
      value: string
    - key: manage_inventory
      value: "true"
      description: Filter the retrieved products' variants.
    - key: allow_backorder
      value: "false"
      description: Filter the retrieved products' variants.
    - key: created_at
      value: "[object Object]"
      description: Filter the retrieved products' variants.
    - key: updated_at
      value: "[object Object]"
      description: Filter the retrieved products' variants.
    - key: deleted_at
      value: "[object Object]"
      description: Filter the retrieved products' variants.
    - key: ean
      value: string
      description: Filter the retrieved products' variants.
    - key: upc
      value: string
      description: Filter the retrieved products' variants.
    - key: barcode
      value: string
      description: Filter the retrieved products' variants.
    - key: with_deleted
      value: "true"
      description: Filter the retrieved products' variants.
response:
  statusCode: 404
  statusText: Not Found
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "message": "Entity with id 1 was not found",
        "type": "not_found"
      }
order: 4000
