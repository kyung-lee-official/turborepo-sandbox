$kind: http-request
description: Manage a product's variant's inventoris to associate them with
  inventory items, update their inventory items, or delete their association
  with inventory items.
url: "{{baseUrl}}/admin/products/:id/variants/inventory-items/batch"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
pathVariables:
  - key: id
    value: string
    description: The product's ID.
body:
  type: json
  content: |-
    {
      "create": [
        {
          "variant_id": "string",
          "required_quantity": 1883.0298597287776,
          "inventory_item_id": "string"
        },
        {
          "variant_id": "string",
          "required_quantity": 7008.16948640474,
          "inventory_item_id": "string"
        }
      ],
      "update": [
        {
          "variant_id": "string",
          "required_quantity": 3646.7703728650067,
          "inventory_item_id": "string"
        },
        {
          "variant_id": "string",
          "required_quantity": 6404.634418387241,
          "inventory_item_id": "string"
        }
      ],
      "delete": [
        {
          "variant_id": "string",
          "inventory_item_id": "string"
        },
        {
          "variant_id": "string",
          "inventory_item_id": "string"
        }
      ]
    }
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Manage Variants Inventory in a Product.resources/examples
order: 1000
