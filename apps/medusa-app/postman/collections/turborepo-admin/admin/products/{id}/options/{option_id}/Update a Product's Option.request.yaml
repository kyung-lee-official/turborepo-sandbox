$kind: http-request
description: Update the details of a product option.
url: "{{baseUrl}}/admin/products/:id/options/:option_id?fields=string"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
queryParams:
  - key: fields
    value: string
    description: Comma-separated fields that should be included in the returned
      data. if a field is prefixed with `+` it will be added to the default
      fields, using `-` will remove it from the default fields. without prefix
      it will replace the entire default fields.
pathVariables:
  - key: id
    value: string
    description: The product's ID.
  - key: option_id
    value: string
    description: The option's ID.
body:
  type: json
  content: |-
    {
      "title": "string",
      "values": [
        "string",
        "string"
      ],
      "additional_data": {}
    }
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Update a Product's Option.resources/examples
order: 2000
