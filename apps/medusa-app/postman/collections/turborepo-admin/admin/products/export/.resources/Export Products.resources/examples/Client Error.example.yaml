$kind: http-example
request:
  url: "{{baseUrl}}/admin/products/export?fields=string&id=string&options=[object
    Object]&title=string&status=draft&sales_channel_id=string&$eq=string&$ne=st\
    ring&$in=string,string&$nin=string,string&$not=string&$gt=string&$gte=strin\
    g&$lt=string&$lte=string&$like=string&$re=string&$ilike=string&$fulltext=st\
    ring&$overlap=string,string&$contains=string,string&$contained=string,strin\
    g&$exists=false&handle=string&is_giftcard=false&collection_id=string&type_i\
    d=string&order=string&q=string&category_id=string&limit=3050.1607324519186&\
    offset=3157.9117089204024&with_deleted=true&price_list_id=string"
  method: POST
  headers:
    - key: Accept
      value: application/json
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    - key: fields
      value: string
      description: Comma-separated fields that should be included in the returned
        data. if a field is prefixed with `+` it will be added to the default
        fields, using `-` will remove it from the default fields. without prefix
        it will replace the entire default fields.
    - key: id
      value: string
      description: Filter products by their tags.
    - key: options
      value: "[object Object]"
      description: Filter the products' variants.
    - key: title
      value: string
    - key: status
      value: draft
    - key: sales_channel_id
      value: string
    - key: $eq
      value: string
      description: Filter by a product's creation date.
    - key: $ne
      value: string
      description: Filter by a product's creation date.
    - key: $in
      value: string,string
      description: Filter by a product's creation date.
    - key: $nin
      value: string,string
      description: Filter by a product's creation date.
    - key: $not
      value: string
      description: Filter by a product's creation date.
    - key: $gt
      value: string
      description: Filter by a product's creation date.
    - key: $gte
      value: string
      description: Filter by a product's creation date.
    - key: $lt
      value: string
      description: Filter by a product's creation date.
    - key: $lte
      value: string
      description: Filter by a product's creation date.
    - key: $like
      value: string
      description: Filter by a product's creation date.
    - key: $re
      value: string
      description: Filter by a product's creation date.
    - key: $ilike
      value: string
      description: Filter by a product's creation date.
    - key: $fulltext
      value: string
      description: Filter by a product's creation date.
    - key: $overlap
      value: string,string
      description: Filter by a product's creation date.
    - key: $contains
      value: string,string
      description: Filter by a product's creation date.
    - key: $contained
      value: string,string
      description: Filter by a product's creation date.
    - key: $exists
      value: "false"
      description: Filter by a product's creation date.
    - key: handle
      value: string
    - key: is_giftcard
      value: "false"
      description: Filter products by whether they are gift cards.
    - key: collection_id
      value: string
    - key: type_id
      value: string
    - key: order
      value: string
      description: The field to sort the data by. By default, the sort order is
        ascending. To change the order to descending, prefix the field name with
        `-`.
    - key: q
      value: string
      description: The product's q.
    - key: category_id
      value: string
    - key: limit
      value: "3050.1607324519186"
      description: Limit the number of items returned in the list.
    - key: offset
      value: "3157.9117089204024"
      description: The number of items to skip when retrieving a list.
    - key: with_deleted
      value: "true"
      description: The product's with deleted.
    - key: price_list_id
      value: string
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "message": "Discount must be set to dynamic",
        "type": "not_allowed"
      }
order: 2000
