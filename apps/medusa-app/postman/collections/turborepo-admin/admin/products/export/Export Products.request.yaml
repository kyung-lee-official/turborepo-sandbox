$kind: http-request
description: >
  Start a product export process to retrieve a CSV of exported products.


  You'll receive in the response the transaction ID of the workflow generating
  the CSV file. To check the status of the execution, send a GET request to
  `/admin/workflows-executions/export-products/:transaction-id`.

  Once the execution finishes successfully, a notification is created for the
  export. You can retrieve the notifications using the `/admin/notification` API
  route to retrieve the file's download URL.
url: "{{baseUrl}}/admin/products/export?fields=string&id=string,string&options=\
  [object
  Object]&title=string&status=draft&id=string&sales_channel_id=string&$eq=strin\
  g&$ne=string&$in=string,string&$nin=string,string&$not=string&$gt=string&$gte\
  =string&$lt=string&$lte=string&$like=string&$re=string&$ilike=string&$fulltex\
  t=string&$overlap=string,string&$contains=string,string&$contained=string,str\
  ing&$exists=true&$eq=string&$ne=string&$in=string,string&$nin=string,string&$\
  not=string&$gt=string&$gte=string&$lt=string&$lte=string&$like=string&$re=str\
  ing&$ilike=string&$fulltext=string&$overlap=string,string&$contains=string,st\
  ring&$contained=string,string&$exists=true&handle=string&is_giftcard=false&co\
  llection_id=string&type_id=string&$eq=string&$ne=string&$in=string,string&$ni\
  n=string,string&$not=string&$gt=string&$gte=string&$lt=string&$lte=string&$li\
  ke=string&$re=string&$ilike=string&$fulltext=string&$overlap=string,string&$c\
  ontains=string,string&$contained=string,string&$exists=false&order=string&q=s\
  tring&category_id=string&limit=3050.1607324519186&offset=3157.9117089204024&w\
  ith_deleted=true&price_list_id=string"
method: POST
headers:
  Accept: application/json
queryParams:
  - key: fields
    value: string
    description: Comma-separated fields that should be included in the returned
      data. if a field is prefixed with `+` it will be added to the default
      fields, using `-` will remove it from the default fields. without prefix
      it will replace the entire default fields.
  - key: id
    value: string,string
    description: Filter products by their tags.
  - key: options
    value: "[object Object]"
    description: Filter the products' variants.
  - key: title
    value: string
  - key: status
    value: draft
  - key: id
    value: string
  - key: sales_channel_id
    value: string
  - key: $eq
    value: string
    description: Filter by a product's creation date.
  - key: $ne
    value: string
    description: Filter by a product's creation date.
  - key: $in
    value: string,string
    description: Filter by a product's creation date.
  - key: $nin
    value: string,string
    description: Filter by a product's creation date.
  - key: $not
    value: string
    description: Filter by a product's creation date.
  - key: $gt
    value: string
    description: Filter by a product's creation date.
  - key: $gte
    value: string
    description: Filter by a product's creation date.
  - key: $lt
    value: string
    description: Filter by a product's creation date.
  - key: $lte
    value: string
    description: Filter by a product's creation date.
  - key: $like
    value: string
    description: Filter by a product's creation date.
  - key: $re
    value: string
    description: Filter by a product's creation date.
  - key: $ilike
    value: string
    description: Filter by a product's creation date.
  - key: $fulltext
    value: string
    description: Filter by a product's creation date.
  - key: $overlap
    value: string,string
    description: Filter by a product's creation date.
  - key: $contains
    value: string,string
    description: Filter by a product's creation date.
  - key: $contained
    value: string,string
    description: Filter by a product's creation date.
  - key: $exists
    value: "true"
    description: Filter by a product's creation date.
  - key: $eq
    value: string
    description: Filter by a product's update date.
  - key: $ne
    value: string
    description: Filter by a product's update date.
  - key: $in
    value: string,string
    description: Filter by a product's update date.
  - key: $nin
    value: string,string
    description: Filter by a product's update date.
  - key: $not
    value: string
    description: Filter by a product's update date.
  - key: $gt
    value: string
    description: Filter by a product's update date.
  - key: $gte
    value: string
    description: Filter by a product's update date.
  - key: $lt
    value: string
    description: Filter by a product's update date.
  - key: $lte
    value: string
    description: Filter by a product's update date.
  - key: $like
    value: string
    description: Filter by a product's update date.
  - key: $re
    value: string
    description: Filter by a product's update date.
  - key: $ilike
    value: string
    description: Filter by a product's update date.
  - key: $fulltext
    value: string
    description: Filter by a product's update date.
  - key: $overlap
    value: string,string
    description: Filter by a product's update date.
  - key: $contains
    value: string,string
    description: Filter by a product's update date.
  - key: $contained
    value: string,string
    description: Filter by a product's update date.
  - key: $exists
    value: "true"
    description: Filter by a product's update date.
  - key: handle
    value: string
  - key: is_giftcard
    value: "false"
    description: Filter products by whether they are gift cards.
  - key: collection_id
    value: string
  - key: type_id
    value: string
  - key: $eq
    value: string
    description: Filter by a product's deletion date.
  - key: $ne
    value: string
    description: Filter by a product's deletion date.
  - key: $in
    value: string,string
    description: Filter by a product's deletion date.
  - key: $nin
    value: string,string
    description: Filter by a product's deletion date.
  - key: $not
    value: string
    description: Filter by a product's deletion date.
  - key: $gt
    value: string
    description: Filter by a product's deletion date.
  - key: $gte
    value: string
    description: Filter by a product's deletion date.
  - key: $lt
    value: string
    description: Filter by a product's deletion date.
  - key: $lte
    value: string
    description: Filter by a product's deletion date.
  - key: $like
    value: string
    description: Filter by a product's deletion date.
  - key: $re
    value: string
    description: Filter by a product's deletion date.
  - key: $ilike
    value: string
    description: Filter by a product's deletion date.
  - key: $fulltext
    value: string
    description: Filter by a product's deletion date.
  - key: $overlap
    value: string,string
    description: Filter by a product's deletion date.
  - key: $contains
    value: string,string
    description: Filter by a product's deletion date.
  - key: $contained
    value: string,string
    description: Filter by a product's deletion date.
  - key: $exists
    value: "false"
    description: Filter by a product's deletion date.
  - key: order
    value: string
    description: The field to sort the data by. By default, the sort order is
      ascending. To change the order to descending, prefix the field name with
      `-`.
  - key: q
    value: string
    description: The product's q.
  - key: category_id
    value: string
  - key: limit
    value: "3050.1607324519186"
    description: Limit the number of items returned in the list.
  - key: offset
    value: "3157.9117089204024"
    description: The number of items to skip when retrieving a list.
  - key: with_deleted
    value: "true"
    description: The product's with deleted.
  - key: price_list_id
    value: string
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Export Products.resources/examples
order: 1000
