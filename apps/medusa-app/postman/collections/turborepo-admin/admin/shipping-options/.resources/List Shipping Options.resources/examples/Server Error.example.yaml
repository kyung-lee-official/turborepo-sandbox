$kind: http-example
request:
  url: "{{baseUrl}}/admin/shipping-options?fields=string&offset=3157.911708920402\
    4&limit=3050.1607324519186&order=string&id=string&q=string&service_zone_id=\
    string&shipping_profile_id=string&provider_id=string&shipping_option_type_i\
    d=string&$eq=string&$ne=string&$in=string,string&$nin=string,string&$not=st\
    ring&$gt=string&$gte=string&$lt=string&$lte=string&$like=string&$re=string&\
    $ilike=string&$fulltext=string&$overlap=string,string&$contains=string,stri\
    ng&$contained=string,string&$exists=true&stock_location_id=string&is_return\
    =false&admin_only=true&with_deleted=true"
  method: GET
  headers:
    - key: Accept
      value: application/json
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    - key: fields
      value: string
      description: Comma-separated fields that should be included in the returned
        data. if a field is prefixed with `+` it will be added to the default
        fields, using `-` will remove it from the default fields. without prefix
        it will replace the entire default fields.
    - key: offset
      value: "3157.9117089204024"
      description: The number of items to skip when retrieving a list.
    - key: limit
      value: "3050.1607324519186"
      description: Limit the number of items returned in the list.
    - key: order
      value: string
      description: The field to sort the data by. By default, the sort order is
        ascending. To change the order to descending, prefix the field name with
        `-`.
    - key: id
      value: string
    - key: q
      value: string
      description: Search term to filter the shipping option's searchable properties.
    - key: service_zone_id
      value: string
    - key: shipping_profile_id
      value: string
    - key: provider_id
      value: string
    - key: shipping_option_type_id
      value: string
    - key: $eq
      value: string
      description: Filter by a shipping option's creation date.
    - key: $ne
      value: string
      description: Filter by a shipping option's creation date.
    - key: $in
      value: string,string
      description: Filter by a shipping option's creation date.
    - key: $nin
      value: string,string
      description: Filter by a shipping option's creation date.
    - key: $not
      value: string
      description: Filter by a shipping option's creation date.
    - key: $gt
      value: string
      description: Filter by a shipping option's creation date.
    - key: $gte
      value: string
      description: Filter by a shipping option's creation date.
    - key: $lt
      value: string
      description: Filter by a shipping option's creation date.
    - key: $lte
      value: string
      description: Filter by a shipping option's creation date.
    - key: $like
      value: string
      description: Filter by a shipping option's creation date.
    - key: $re
      value: string
      description: Filter by a shipping option's creation date.
    - key: $ilike
      value: string
      description: Filter by a shipping option's creation date.
    - key: $fulltext
      value: string
      description: Filter by a shipping option's creation date.
    - key: $overlap
      value: string,string
      description: Filter by a shipping option's creation date.
    - key: $contains
      value: string,string
      description: Filter by a shipping option's creation date.
    - key: $contained
      value: string,string
      description: Filter by a shipping option's creation date.
    - key: $exists
      value: "true"
      description: Filter by a shipping option's creation date.
    - key: stock_location_id
      value: string
    - key: is_return
      value: "false"
      description: Filter by whether the shipping option is used for returns.
    - key: admin_only
      value: "true"
      description: Filter by whether the shipping option is used by admin users only.
    - key: with_deleted
      value: "true"
      description: Whether to include deleted records in the result.
response:
  statusCode: 500
  statusText: Internal Server Error
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "code": "api_error",
        "message": "An error occured while hashing password",
        "type": "database_error"
      }
order: 7000
