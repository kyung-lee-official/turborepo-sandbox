$kind: http-example
request:
  url: "{{baseUrl}}/admin/product-categories?fields=string&include_ancestors_tree\
    =true&include_descendants_tree=true"
  method: POST
  headers:
    - key: Content-Type
      value: application/json
    - key: Accept
      value: application/json
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    - key: fields
      value: string
      description: Comma-separated fields that should be included in the returned
        data. if a field is prefixed with `+` it will be added to the default
        fields, using `-` will remove it from the default fields. without prefix
        it will replace the entire default fields. This API route restricts the
        fields that can be selected. Learn how to override the retrievable
        fields in the [Retrieve Custom
        Links](https://docs.medusajs.com/learn/fundamentals/api-routes/retrieve-custom-links)
        documentation.
    - key: include_ancestors_tree
      value: "true"
      description: Whether to retrieve the category's parent. If you enable this, add
        to the `fields` query parameter `parent_category` to set the parent of a
        category in this field. You can either pass `*parent_category` to
        retreieve the fields of all parent categories, or select specific fields
        to make the response size smaller. For example,
        `fields=parent_category.id,parent_category.name`.
    - key: include_descendants_tree
      value: "true"
      description: Whether to retrieve a list of child categories. If you enable this,
        add to the `fields` query parameter `category_children` to set the child
        of a category in this field. You can either pass `*category_children` to
        retreieve the fields of all child categories, or select specific fields
        to make the response size smaller. For example,
        `fields=category_children.id,category_children.name`.
  body:
    type: json
    content: |-
      {
        "name": "string",
        "description": "string",
        "handle": "string",
        "is_internal": false,
        "is_active": false,
        "parent_category_id": "string",
        "rank": 8881.496108718255,
        "metadata": {}
      }
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "product_category": {
          "parent_category": {},
          "name": "string",
          "description": "string",
          "id": "string",
          "created_at": "1980-03-08T10:15:59.476Z",
          "updated_at": "1973-06-01T03:25:31.313Z",
          "handle": "string",
          "deleted_at": "1950-01-10T03:51:50.962Z",
          "is_active": false,
          "is_internal": false,
          "rank": 4791.742077628985,
          "parent_category_id": "string",
          "metadata": {}
        }
      }
order: 1000
