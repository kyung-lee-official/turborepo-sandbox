$kind: http-example
request:
  url: "{{baseUrl}}/admin/tax-providers?fields=string&offset=3157.9117089204024&l\
    imit=3050.1607324519186&order=string&id=string&is_enabled=false&with_delete\
    d=true"
  method: GET
  headers:
    - key: Accept
      value: application/json
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    - key: fields
      value: string
      description: >-
        Comma-separated fields that should be included in the returned data.

        if a field is prefixed with `+` it will be added to the default fields,
        using `-` will remove it from the default fields.

        without prefix it will replace the entire default fields.
    - key: offset
      value: "3157.9117089204024"
      description: The number of items to skip when retrieving a list.
    - key: limit
      value: "3050.1607324519186"
      description: Limit the number of items returned in the list.
    - key: order
      value: string
      description: The field to sort the data by. By default, the sort order is
        ascending. To change the order to descending, prefix the field name with
        `-`.
    - key: id
      value: string
    - key: is_enabled
      value: "false"
      description: Filter by whether the tax provider is enabled.
    - key: with_deleted
      value: "true"
      description: Whether to include deleted records in the result.
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "count": 257.643442822848,
        "limit": 3344.040552675749,
        "offset": 2548.8757838608867,
        "tax_providers": [
          {
            "id": "tp_taxjar_taxjar",
            "is_enabled": false
          },
          {
            "id": "tp_taxjar_taxjar",
            "is_enabled": false
          }
        ],
        "estimate_count": 5779.516389972261
      }
order: 1000
