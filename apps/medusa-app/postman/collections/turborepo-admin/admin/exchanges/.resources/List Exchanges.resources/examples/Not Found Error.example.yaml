$kind: http-example
request:
  url: "{{baseUrl}}/admin/exchanges?fields=string&offset=3157.9117089204024&limit\
    =3050.1607324519186&order=string&$eq=string&$ne=string&$in=string,string&$n\
    in=string,string&$not=string&$gt=string&$gte=string&$lt=string&$lte=string&\
    $like=string&$re=string&$ilike=string&$fulltext=string&$overlap=string,stri\
    ng&$contains=string,string&$contained=string,string&$exists=true&id=string&\
    order_id=string&status=string&with_deleted=true"
  method: GET
  headers:
    - key: Accept
      value: application/json
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    - key: fields
      value: string
      description: Comma-separated fields that should be included in the returned
        data. if a field is prefixed with `+` it will be added to the default
        fields, using `-` will remove it from the default fields. without prefix
        it will replace the entire default fields.
    - key: offset
      value: "3157.9117089204024"
      description: The number of items to skip when retrieving a list.
    - key: limit
      value: "3050.1607324519186"
      description: Limit the number of items returned in the list.
    - key: order
      value: string
      description: The field to sort the data by. By default, the sort order is
        ascending. To change the order to descending, prefix the field name with
        `-`.
    - key: $eq
      value: string
      description: Filter by an exchange's deletion date.
    - key: $ne
      value: string
      description: Filter by an exchange's deletion date.
    - key: $in
      value: string,string
      description: Filter by an exchange's deletion date.
    - key: $nin
      value: string,string
      description: Filter by an exchange's deletion date.
    - key: $not
      value: string
      description: Filter by an exchange's deletion date.
    - key: $gt
      value: string
      description: Filter by an exchange's deletion date.
    - key: $gte
      value: string
      description: Filter by an exchange's deletion date.
    - key: $lt
      value: string
      description: Filter by an exchange's deletion date.
    - key: $lte
      value: string
      description: Filter by an exchange's deletion date.
    - key: $like
      value: string
      description: Filter by an exchange's deletion date.
    - key: $re
      value: string
      description: Filter by an exchange's deletion date.
    - key: $ilike
      value: string
      description: Filter by an exchange's deletion date.
    - key: $fulltext
      value: string
      description: Filter by an exchange's deletion date.
    - key: $overlap
      value: string,string
      description: Filter by an exchange's deletion date.
    - key: $contains
      value: string,string
      description: Filter by an exchange's deletion date.
    - key: $contained
      value: string,string
      description: Filter by an exchange's deletion date.
    - key: $exists
      value: "true"
      description: Filter by an exchange's deletion date.
    - key: id
      value: string
    - key: order_id
      value: string
    - key: status
      value: string
    - key: with_deleted
      value: "true"
      description: Whether to include deleted records in the result.
response:
  statusCode: 404
  statusText: Not Found
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "message": "Entity with id 1 was not found",
        "type": "not_found"
      }
order: 4000
