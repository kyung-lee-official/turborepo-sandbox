$kind: http-example
request:
  url: "{{baseUrl}}/admin/tax-rates?fields=string&offset=3157.9117089204024&limit\
    =3050.1607324519186&order=string&q=string&tax_region_id=string&is_default=t\
    rue&$eq=string&$ne=string&$in=string,string&$nin=string,string&$not=string&\
    $gt=string&$gte=string&$lt=string&$lte=string&$like=string&$re=string&$ilik\
    e=string&$fulltext=string&$overlap=string,string&$contains=string,string&$c\
    ontained=string,string&$exists=false&service_zone_id=string&shipping_profil\
    e_id=string&provider_id=string&shipping_option_type_id=string&with_deleted=\
    true"
  method: GET
  headers:
    - key: Accept
      value: application/json
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    - key: fields
      value: string
      description: Comma-separated fields that should be included in the returned
        data. if a field is prefixed with `+` it will be added to the default
        fields, using `-` will remove it from the default fields. without prefix
        it will replace the entire default fields.
    - key: offset
      value: "3157.9117089204024"
      description: The number of items to skip when retrieving a list.
    - key: limit
      value: "3050.1607324519186"
      description: Limit the number of items returned in the list.
    - key: order
      value: string
      description: The field to sort the data by. By default, the sort order is
        ascending. To change the order to descending, prefix the field name with
        `-`.
    - key: q
      value: string
      description: Search terms to filter the tax rate's searchable properties.
    - key: tax_region_id
      value: string
    - key: is_default
      value: "true"
      description: Filter by whether the tax rate is a default in its tax region.
    - key: $eq
      value: string
      description: Filter by a tax rate's creation date.
    - key: $ne
      value: string
      description: Filter by a tax rate's creation date.
    - key: $in
      value: string,string
      description: Filter by a tax rate's creation date.
    - key: $nin
      value: string,string
      description: Filter by a tax rate's creation date.
    - key: $not
      value: string
      description: Filter by a tax rate's creation date.
    - key: $gt
      value: string
      description: Filter by a tax rate's creation date.
    - key: $gte
      value: string
      description: Filter by a tax rate's creation date.
    - key: $lt
      value: string
      description: Filter by a tax rate's creation date.
    - key: $lte
      value: string
      description: Filter by a tax rate's creation date.
    - key: $like
      value: string
      description: Filter by a tax rate's creation date.
    - key: $re
      value: string
      description: Filter by a tax rate's creation date.
    - key: $ilike
      value: string
      description: Filter by a tax rate's creation date.
    - key: $fulltext
      value: string
      description: Filter by a tax rate's creation date.
    - key: $overlap
      value: string,string
      description: Filter by a tax rate's creation date.
    - key: $contains
      value: string,string
      description: Filter by a tax rate's creation date.
    - key: $contained
      value: string,string
      description: Filter by a tax rate's creation date.
    - key: $exists
      value: "false"
      description: Filter by a tax rate's creation date.
    - key: service_zone_id
      value: string
      description: Filter by a service zone's ID to retrieve its associated tax rates.
    - key: shipping_profile_id
      value: string
      description: Filter by a shipping profile's ID to retrieve its associated tax rates.
    - key: provider_id
      value: string
      description: Filter by a fulfillment provider's ID to retrieve its associated
        tax rates.
    - key: shipping_option_type_id
      value: string
      description: Filter by a shipping option type's ID to retrieve its associated
        tax rates.
    - key: with_deleted
      value: "true"
      description: Whether to include deleted records in the result.
response:
  statusCode: 404
  statusText: Not Found
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "message": "Entity with id 1 was not found",
        "type": "not_found"
      }
order: 4000
