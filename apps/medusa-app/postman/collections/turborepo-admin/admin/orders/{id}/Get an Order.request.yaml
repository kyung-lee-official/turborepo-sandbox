$kind: http-request
description: Retrieve an order by its ID. You can expand the order's relations
  or select the fields that should be returned.
url: "{{baseUrl}}/admin/orders/:id?fields=string&version=8661.135667642375"
method: GET
headers:
  Accept: application/json
queryParams:
  - key: fields
    value: string
    description: Comma-separated fields that should be included in the returned
      data. if a field is prefixed with `+` it will be added to the default
      fields, using `-` will remove it from the default fields. without prefix
      it will replace the entire default fields.
  - key: version
    value: "8661.135667642375"
    description: The order's version.
pathVariables:
  - key: id
    value: string
    description: The order's ID.
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Get an Order.resources/examples
order: 11000
