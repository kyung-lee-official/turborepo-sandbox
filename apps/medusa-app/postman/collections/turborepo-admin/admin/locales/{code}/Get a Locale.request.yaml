$kind: http-request
description: Retrieve a locale by its code. You can expand the locale's
  relations or select the fields that should be returned.
url: "{{baseUrl}}/admin/locales/:code?fields=string"
method: GET
headers:
  Accept: application/json
queryParams:
  - key: fields
    value: string
    description: Comma-separated fields that should be included in the returned
      data. If a field is prefixed with `+` it will be added to the default
      fields, using `-` will remove it from the default fields. Without prefix
      it will replace the entire default fields.
pathVariables:
  - key: code
    value: fr-FR
    description: The locale's code in [BCP 47
      format](https://gist.github.com/typpo/b2b828a35e683b9bf8db91b5404f1bd1).
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Get a Locale.resources/examples
order: 1000
