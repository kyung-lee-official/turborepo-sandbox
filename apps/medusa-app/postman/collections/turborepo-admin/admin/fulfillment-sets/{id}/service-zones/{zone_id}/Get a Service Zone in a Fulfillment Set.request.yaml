$kind: http-request
description: Retrieve a service zone that belongs to a fulfillment set. be paginated.
url: "{{baseUrl}}/admin/fulfillment-sets/:id/service-zones/:zone_id?fields=string"
method: GET
headers:
  Accept: application/json
queryParams:
  - key: fields
    value: string
    description: Comma-separated fields that should be included in the returned
      data. if a field is prefixed with `+` it will be added to the default
      fields, using `-` will remove it from the default fields. without prefix
      it will replace the entire default fields.
pathVariables:
  - key: id
    value: string
    description: The fulfillment set's ID.
  - key: zone_id
    value: string
    description: The service zone's ID.
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Get a Service Zone in a Fulfillment Set.resources/examples
order: 1000
