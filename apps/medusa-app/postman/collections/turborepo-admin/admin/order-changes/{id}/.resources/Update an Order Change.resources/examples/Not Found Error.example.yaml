$kind: http-example
request:
  url: "{{baseUrl}}/admin/order-changes/:id?$eq=string&$ne=string&$in=string,stri\
    ng&$nin=string,string&$not=string&$gt=string&$gte=string&$lt=string&$lte=st\
    ring&$like=string&$re=string&$ilike=string&$fulltext=string&$overlap=string\
    ,string&$contains=string,string&$contained=string,string&$exists=true&id=st\
    ring&status=string&change_type=string&fields=string"
  method: POST
  headers:
    - key: Content-Type
      value: application/json
    - key: Accept
      value: application/json
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    - key: $eq
      value: string
      description: Filter by the order change's creation date.
    - key: $ne
      value: string
      description: Filter by the order change's creation date.
    - key: $in
      value: string,string
      description: Filter by the order change's creation date.
    - key: $nin
      value: string,string
      description: Filter by the order change's creation date.
    - key: $not
      value: string
      description: Filter by the order change's creation date.
    - key: $gt
      value: string
      description: Filter by the order change's creation date.
    - key: $gte
      value: string
      description: Filter by the order change's creation date.
    - key: $lt
      value: string
      description: Filter by the order change's creation date.
    - key: $lte
      value: string
      description: Filter by the order change's creation date.
    - key: $like
      value: string
      description: Filter by the order change's creation date.
    - key: $re
      value: string
      description: Filter by the order change's creation date.
    - key: $ilike
      value: string
      description: Filter by the order change's creation date.
    - key: $fulltext
      value: string
      description: Filter by the order change's creation date.
    - key: $overlap
      value: string,string
      description: Filter by the order change's creation date.
    - key: $contains
      value: string,string
      description: Filter by the order change's creation date.
    - key: $contained
      value: string,string
      description: Filter by the order change's creation date.
    - key: $exists
      value: "true"
      description: Filter by the order change's creation date.
    - key: id
      value: string
    - key: status
      value: string
    - key: change_type
      value: string
    - key: fields
      value: string
      description: Comma-separated fields that should be included in the returned
        data. If a field is prefixed with `+` it will be added to the default
        fields, using `-` will remove it from the default fields. Without prefix
        it will replace the entire default fields.
  pathVariables:
    - key: id
      value: string
      description: The order change's ID.
  body:
    type: json
    content: |-
      {
        "carry_over_promotions": false
      }
response:
  statusCode: 404
  statusText: Not Found
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "message": "Entity with id 1 was not found",
        "type": "not_found"
      }
order: 4000
