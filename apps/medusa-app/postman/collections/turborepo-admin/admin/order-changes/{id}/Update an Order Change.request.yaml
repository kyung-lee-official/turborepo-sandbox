$kind: http-request
description: Update an order change's details. An order change can be an
  exchange, claim, or edit. For example, you can edit whether an exchange
  carries over the parent order's promotion.
url: "{{baseUrl}}/admin/order-changes/:id?$eq=string&$ne=string&$in=string,stri\
  ng&$nin=string,string&$not=string&$gt=string&$gte=string&$lt=string&$lte=stri\
  ng&$like=string&$re=string&$ilike=string&$fulltext=string&$overlap=string,str\
  ing&$contains=string,string&$contained=string,string&$exists=true&$eq=string&\
  $ne=string&$in=string,string&$nin=string,string&$not=string&$gt=string&$gte=s\
  tring&$lt=string&$lte=string&$like=string&$re=string&$ilike=string&$fulltext=\
  string&$overlap=string,string&$contains=string,string&$contained=string,strin\
  g&$exists=false&$eq=string&$ne=string&$in=string,string&$nin=string,string&$n\
  ot=string&$gt=string&$gte=string&$lt=string&$lte=string&$like=string&$re=stri\
  ng&$ilike=string&$fulltext=string&$overlap=string,string&$contains=string,str\
  ing&$contained=string,string&$exists=true&id=string&status=string&change_type\
  =string&fields=string"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
queryParams:
  - key: $eq
    value: string
    description: Filter by the order change's creation date.
  - key: $ne
    value: string
    description: Filter by the order change's creation date.
  - key: $in
    value: string,string
    description: Filter by the order change's creation date.
  - key: $nin
    value: string,string
    description: Filter by the order change's creation date.
  - key: $not
    value: string
    description: Filter by the order change's creation date.
  - key: $gt
    value: string
    description: Filter by the order change's creation date.
  - key: $gte
    value: string
    description: Filter by the order change's creation date.
  - key: $lt
    value: string
    description: Filter by the order change's creation date.
  - key: $lte
    value: string
    description: Filter by the order change's creation date.
  - key: $like
    value: string
    description: Filter by the order change's creation date.
  - key: $re
    value: string
    description: Filter by the order change's creation date.
  - key: $ilike
    value: string
    description: Filter by the order change's creation date.
  - key: $fulltext
    value: string
    description: Filter by the order change's creation date.
  - key: $overlap
    value: string,string
    description: Filter by the order change's creation date.
  - key: $contains
    value: string,string
    description: Filter by the order change's creation date.
  - key: $contained
    value: string,string
    description: Filter by the order change's creation date.
  - key: $exists
    value: "true"
    description: Filter by the order change's creation date.
  - key: $eq
    value: string
    description: Filter by the order change's update date.
  - key: $ne
    value: string
    description: Filter by the order change's update date.
  - key: $in
    value: string,string
    description: Filter by the order change's update date.
  - key: $nin
    value: string,string
    description: Filter by the order change's update date.
  - key: $not
    value: string
    description: Filter by the order change's update date.
  - key: $gt
    value: string
    description: Filter by the order change's update date.
  - key: $gte
    value: string
    description: Filter by the order change's update date.
  - key: $lt
    value: string
    description: Filter by the order change's update date.
  - key: $lte
    value: string
    description: Filter by the order change's update date.
  - key: $like
    value: string
    description: Filter by the order change's update date.
  - key: $re
    value: string
    description: Filter by the order change's update date.
  - key: $ilike
    value: string
    description: Filter by the order change's update date.
  - key: $fulltext
    value: string
    description: Filter by the order change's update date.
  - key: $overlap
    value: string,string
    description: Filter by the order change's update date.
  - key: $contains
    value: string,string
    description: Filter by the order change's update date.
  - key: $contained
    value: string,string
    description: Filter by the order change's update date.
  - key: $exists
    value: "false"
    description: Filter by the order change's update date.
  - key: $eq
    value: string
    description: Filter by the order change's deletion date.
  - key: $ne
    value: string
    description: Filter by the order change's deletion date.
  - key: $in
    value: string,string
    description: Filter by the order change's deletion date.
  - key: $nin
    value: string,string
    description: Filter by the order change's deletion date.
  - key: $not
    value: string
    description: Filter by the order change's deletion date.
  - key: $gt
    value: string
    description: Filter by the order change's deletion date.
  - key: $gte
    value: string
    description: Filter by the order change's deletion date.
  - key: $lt
    value: string
    description: Filter by the order change's deletion date.
  - key: $lte
    value: string
    description: Filter by the order change's deletion date.
  - key: $like
    value: string
    description: Filter by the order change's deletion date.
  - key: $re
    value: string
    description: Filter by the order change's deletion date.
  - key: $ilike
    value: string
    description: Filter by the order change's deletion date.
  - key: $fulltext
    value: string
    description: Filter by the order change's deletion date.
  - key: $overlap
    value: string,string
    description: Filter by the order change's deletion date.
  - key: $contains
    value: string,string
    description: Filter by the order change's deletion date.
  - key: $contained
    value: string,string
    description: Filter by the order change's deletion date.
  - key: $exists
    value: "true"
    description: Filter by the order change's deletion date.
  - key: id
    value: string
  - key: status
    value: string
  - key: change_type
    value: string
  - key: fields
    value: string
    description: Comma-separated fields that should be included in the returned
      data. If a field is prefixed with `+` it will be added to the default
      fields, using `-` will remove it from the default fields. Without prefix
      it will replace the entire default fields.
pathVariables:
  - key: id
    value: string
    description: The order change's ID.
body:
  type: json
  content: |-
    {
      "carry_over_promotions": false
    }
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Update an Order Change.resources/examples
order: 1000
