$kind: http-request
description: Retrieve a list of transaction groups. The transaction groups can
  be filtered by fields such as `id`. The transaction groups can also be sorted
  or paginated.
url: "{{baseUrl}}/admin/transaction-groups?id=string&code=string&$eq=string&$ne\
  =string&$in=string,string&$nin=string,string&$not=string&$gt=string&$gte=stri\
  ng&$lt=string&$lte=string&$like=string&$re=string&$ilike=string&$fulltext=str\
  ing&$overlap=string,string&$contains=string,string&$contained=string,string&$\
  exists=true&$eq=string&$ne=string&$in=string,string&$nin=string,string&$not=s\
  tring&$gt=string&$gte=string&$lt=string&$lte=string&$like=string&$re=string&$\
  ilike=string&$fulltext=string&$overlap=string,string&$contains=string,string&\
  $contained=string,string&$exists=false&limit=3050.1607324519186&offset=3157.9\
  117089204024&order=string&fields=string"
method: GET
headers:
  Accept: application/json
queryParams:
  - key: id
    value: string
  - key: code
    value: string
  - key: $eq
    value: string
    description: Filter by a transaction group's creation date.
  - key: $ne
    value: string
    description: Filter by a transaction group's creation date.
  - key: $in
    value: string,string
    description: Filter by a transaction group's creation date.
  - key: $nin
    value: string,string
    description: Filter by a transaction group's creation date.
  - key: $not
    value: string
    description: Filter by a transaction group's creation date.
  - key: $gt
    value: string
    description: Filter by a transaction group's creation date.
  - key: $gte
    value: string
    description: Filter by a transaction group's creation date.
  - key: $lt
    value: string
    description: Filter by a transaction group's creation date.
  - key: $lte
    value: string
    description: Filter by a transaction group's creation date.
  - key: $like
    value: string
    description: Filter by a transaction group's creation date.
  - key: $re
    value: string
    description: Filter by a transaction group's creation date.
  - key: $ilike
    value: string
    description: Filter by a transaction group's creation date.
  - key: $fulltext
    value: string
    description: Filter by a transaction group's creation date.
  - key: $overlap
    value: string,string
    description: Filter by a transaction group's creation date.
  - key: $contains
    value: string,string
    description: Filter by a transaction group's creation date.
  - key: $contained
    value: string,string
    description: Filter by a transaction group's creation date.
  - key: $exists
    value: "true"
    description: Filter by a transaction group's creation date.
  - key: $eq
    value: string
    description: Filter by a transaction group's update date.
  - key: $ne
    value: string
    description: Filter by a transaction group's update date.
  - key: $in
    value: string,string
    description: Filter by a transaction group's update date.
  - key: $nin
    value: string,string
    description: Filter by a transaction group's update date.
  - key: $not
    value: string
    description: Filter by a transaction group's update date.
  - key: $gt
    value: string
    description: Filter by a transaction group's update date.
  - key: $gte
    value: string
    description: Filter by a transaction group's update date.
  - key: $lt
    value: string
    description: Filter by a transaction group's update date.
  - key: $lte
    value: string
    description: Filter by a transaction group's update date.
  - key: $like
    value: string
    description: Filter by a transaction group's update date.
  - key: $re
    value: string
    description: Filter by a transaction group's update date.
  - key: $ilike
    value: string
    description: Filter by a transaction group's update date.
  - key: $fulltext
    value: string
    description: Filter by a transaction group's update date.
  - key: $overlap
    value: string,string
    description: Filter by a transaction group's update date.
  - key: $contains
    value: string,string
    description: Filter by a transaction group's update date.
  - key: $contained
    value: string,string
    description: Filter by a transaction group's update date.
  - key: $exists
    value: "false"
    description: Filter by a transaction group's update date.
  - key: limit
    value: "3050.1607324519186"
    description: Limit the number of items returned in the list.
  - key: offset
    value: "3157.9117089204024"
    description: The number of items to skip when retrieving a list.
  - key: order
    value: string
    description: The field to sort the data by. By default, the sort order is
      ascending. To change the order to descending, prefix the field name with
      `-`.
  - key: fields
    value: string
    description: Comma-separated fields that should be included in the returned
      data. If a field is prefixed with `+` it will be added to the default
      fields, using `-` will remove it from the default fields. Without prefix
      it will replace the entire default fields.
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/List Transaction Groups.resources/examples
order: 1000
