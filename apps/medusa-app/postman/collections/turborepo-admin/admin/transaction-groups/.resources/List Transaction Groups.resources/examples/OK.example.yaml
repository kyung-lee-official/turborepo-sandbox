$kind: http-example
request:
  url: "{{baseUrl}}/admin/transaction-groups?id=string&code=string&$eq=string&$ne\
    =string&$in=string,string&$nin=string,string&$not=string&$gt=string&$gte=st\
    ring&$lt=string&$lte=string&$like=string&$re=string&$ilike=string&$fulltext\
    =string&$overlap=string,string&$contains=string,string&$contained=string,st\
    ring&$exists=false&limit=3050.1607324519186&offset=3157.9117089204024&order\
    =string&fields=string"
  method: GET
  headers:
    - key: Accept
      value: application/json
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    - key: id
      value: string
    - key: code
      value: string
    - key: $eq
      value: string
      description: Filter by a transaction group's creation date.
    - key: $ne
      value: string
      description: Filter by a transaction group's creation date.
    - key: $in
      value: string,string
      description: Filter by a transaction group's creation date.
    - key: $nin
      value: string,string
      description: Filter by a transaction group's creation date.
    - key: $not
      value: string
      description: Filter by a transaction group's creation date.
    - key: $gt
      value: string
      description: Filter by a transaction group's creation date.
    - key: $gte
      value: string
      description: Filter by a transaction group's creation date.
    - key: $lt
      value: string
      description: Filter by a transaction group's creation date.
    - key: $lte
      value: string
      description: Filter by a transaction group's creation date.
    - key: $like
      value: string
      description: Filter by a transaction group's creation date.
    - key: $re
      value: string
      description: Filter by a transaction group's creation date.
    - key: $ilike
      value: string
      description: Filter by a transaction group's creation date.
    - key: $fulltext
      value: string
      description: Filter by a transaction group's creation date.
    - key: $overlap
      value: string,string
      description: Filter by a transaction group's creation date.
    - key: $contains
      value: string,string
      description: Filter by a transaction group's creation date.
    - key: $contained
      value: string,string
      description: Filter by a transaction group's creation date.
    - key: $exists
      value: "false"
      description: Filter by a transaction group's creation date.
    - key: limit
      value: "3050.1607324519186"
      description: Limit the number of items returned in the list.
    - key: offset
      value: "3157.9117089204024"
      description: The number of items to skip when retrieving a list.
    - key: order
      value: string
      description: The field to sort the data by. By default, the sort order is
        ascending. To change the order to descending, prefix the field name with
        `-`.
    - key: fields
      value: string
      description: Comma-separated fields that should be included in the returned
        data. If a field is prefixed with `+` it will be added to the default
        fields, using `-` will remove it from the default fields. Without prefix
        it will replace the entire default fields.
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "limit": 7129.811784575047,
        "offset": 5342.3597056483295,
        "count": 4164.70604577593,
        "transaction_groups": [
          {
            "account": {},
            "id": "string",
            "code": "string",
            "credits": 3228.190959688797,
            "debits": 9258.264280847434,
            "balance": 7735.085726772457,
            "metadata": {}
          },
          {
            "account": {},
            "id": "string",
            "code": "string",
            "credits": 6922.223592361865,
            "debits": 4868.147049943365,
            "balance": 6628.602718313295,
            "metadata": {}
          }
        ],
        "estimate_count": 3835.407374817612
      }
order: 1000
