$kind: http-example
request:
  url: "{{baseUrl}}/admin/inventory-items?fields=string&offset=3157.9117089204024\
    &limit=3050.1607324519186&order=string&q=string&id=string&sku=string&origin\
    _country=string&mid_code=string&hs_code=string&material=string&requires_shi\
    pping=false&$eq=string&$ne=string&$in=string,string&$nin=string,string&$lik\
    e=string&$ilike=string&$re=string&$contains=string,string&$gt=string&$gte=s\
    tring&$lt=string&$lte=string&location_id=string&with_deleted=true"
  method: GET
  headers:
    - key: Accept
      value: application/json
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    - key: fields
      value: string
      description: Comma-separated fields that should be included in the returned
        data. if a field is prefixed with `+` it will be added to the default
        fields, using `-` will remove it from the default fields. without prefix
        it will replace the entire default fields.
    - key: offset
      value: "3157.9117089204024"
      description: The number of items to skip when retrieving a list.
    - key: limit
      value: "3050.1607324519186"
      description: Limit the number of items returned in the list.
    - key: order
      value: string
      description: The field to sort the data by. By default, the sort order is
        ascending. To change the order to descending, prefix the field name with
        `-`.
    - key: q
      value: string
      description: The inventory item's q.
    - key: id
      value: string
    - key: sku
      value: string
    - key: origin_country
      value: string
    - key: mid_code
      value: string
    - key: hs_code
      value: string
    - key: material
      value: string
    - key: requires_shipping
      value: "false"
      description: The inventory item's requires shipping.
    - key: $eq
      value: string
      description: Filter the inventory item's weight.
    - key: $ne
      value: string
      description: Filter the inventory item's weight.
    - key: $in
      value: string,string
      description: Filter the inventory item's weight.
    - key: $nin
      value: string,string
      description: Filter the inventory item's weight.
    - key: $like
      value: string
      description: Filter the inventory item's weight.
    - key: $ilike
      value: string
      description: Filter the inventory item's weight.
    - key: $re
      value: string
      description: Filter the inventory item's weight.
    - key: $contains
      value: string,string
      description: Filter the inventory item's weight.
    - key: $gt
      value: string
      description: Filter the inventory item's weight.
    - key: $gte
      value: string
      description: Filter the inventory item's weight.
    - key: $lt
      value: string
      description: Filter the inventory item's weight.
    - key: $lte
      value: string
      description: Filter the inventory item's weight.
    - key: location_id
      value: string
      description: Filter by the inventory item's associated location IDs.
    - key: with_deleted
      value: "true"
      description: Whether to include deleted records in the result.
response:
  statusCode: 422
  statusText: Unprocessable Entity (WebDAV) (RFC 4918)
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "code": "invalid_request_error",
        "message": "Discount with code TEST already exists.",
        "type": "duplicate_error"
      }
order: 6000
