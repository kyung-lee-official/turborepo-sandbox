$kind: http-request
description: Retrieve a list of inventory items. The inventory items can be
  filtered by fields such as `id`. The inventory items can also be sorted or
  paginated.
url: "{{baseUrl}}/admin/inventory-items?fields=string&offset=3157.9117089204024\
  &limit=3050.1607324519186&order=string&q=string&id=string&sku=string&origin_c\
  ountry=string&mid_code=string&hs_code=string&material=string&requires_shippin\
  g=false&$eq=string&$ne=string&$in=string,string&$nin=string,string&$like=stri\
  ng&$ilike=string&$re=string&$contains=string,string&$gt=string&$gte=string&$l\
  t=string&$lte=string&$eq=string&$ne=string&$in=string,string&$nin=string,stri\
  ng&$like=string&$ilike=string&$re=string&$contains=string,string&$gt=string&$\
  gte=string&$lt=string&$lte=string&$eq=string&$ne=string&$in=string,string&$ni\
  n=string,string&$like=string&$ilike=string&$re=string&$contains=string,string\
  &$gt=string&$gte=string&$lt=string&$lte=string&$eq=string&$ne=string&$in=stri\
  ng,string&$nin=string,string&$like=string&$ilike=string&$re=string&$contains=\
  string,string&$gt=string&$gte=string&$lt=string&$lte=string&location_id=strin\
  g&with_deleted=true"
method: GET
headers:
  Accept: application/json
queryParams:
  - key: fields
    value: string
    description: Comma-separated fields that should be included in the returned
      data. if a field is prefixed with `+` it will be added to the default
      fields, using `-` will remove it from the default fields. without prefix
      it will replace the entire default fields.
  - key: offset
    value: "3157.9117089204024"
    description: The number of items to skip when retrieving a list.
  - key: limit
    value: "3050.1607324519186"
    description: Limit the number of items returned in the list.
  - key: order
    value: string
    description: The field to sort the data by. By default, the sort order is
      ascending. To change the order to descending, prefix the field name with
      `-`.
  - key: q
    value: string
    description: The inventory item's q.
  - key: id
    value: string
  - key: sku
    value: string
  - key: origin_country
    value: string
  - key: mid_code
    value: string
  - key: hs_code
    value: string
  - key: material
    value: string
  - key: requires_shipping
    value: "false"
    description: The inventory item's requires shipping.
  - key: $eq
    value: string
    description: Filter the inventory item's weight.
  - key: $ne
    value: string
    description: Filter the inventory item's weight.
  - key: $in
    value: string,string
    description: Filter the inventory item's weight.
  - key: $nin
    value: string,string
    description: Filter the inventory item's weight.
  - key: $like
    value: string
    description: Filter the inventory item's weight.
  - key: $ilike
    value: string
    description: Filter the inventory item's weight.
  - key: $re
    value: string
    description: Filter the inventory item's weight.
  - key: $contains
    value: string,string
    description: Filter the inventory item's weight.
  - key: $gt
    value: string
    description: Filter the inventory item's weight.
  - key: $gte
    value: string
    description: Filter the inventory item's weight.
  - key: $lt
    value: string
    description: Filter the inventory item's weight.
  - key: $lte
    value: string
    description: Filter the inventory item's weight.
  - key: $eq
    value: string
    description: Filter the inventory item's length.
  - key: $ne
    value: string
    description: Filter the inventory item's length.
  - key: $in
    value: string,string
    description: Filter the inventory item's length.
  - key: $nin
    value: string,string
    description: Filter the inventory item's length.
  - key: $like
    value: string
    description: Filter the inventory item's length.
  - key: $ilike
    value: string
    description: Filter the inventory item's length.
  - key: $re
    value: string
    description: Filter the inventory item's length.
  - key: $contains
    value: string,string
    description: Filter the inventory item's length.
  - key: $gt
    value: string
    description: Filter the inventory item's length.
  - key: $gte
    value: string
    description: Filter the inventory item's length.
  - key: $lt
    value: string
    description: Filter the inventory item's length.
  - key: $lte
    value: string
    description: Filter the inventory item's length.
  - key: $eq
    value: string
    description: Filter by the inventory item's height.
  - key: $ne
    value: string
    description: Filter by the inventory item's height.
  - key: $in
    value: string,string
    description: Filter by the inventory item's height.
  - key: $nin
    value: string,string
    description: Filter by the inventory item's height.
  - key: $like
    value: string
    description: Filter by the inventory item's height.
  - key: $ilike
    value: string
    description: Filter by the inventory item's height.
  - key: $re
    value: string
    description: Filter by the inventory item's height.
  - key: $contains
    value: string,string
    description: Filter by the inventory item's height.
  - key: $gt
    value: string
    description: Filter by the inventory item's height.
  - key: $gte
    value: string
    description: Filter by the inventory item's height.
  - key: $lt
    value: string
    description: Filter by the inventory item's height.
  - key: $lte
    value: string
    description: Filter by the inventory item's height.
  - key: $eq
    value: string
    description: Filter by the inventory item's width.
  - key: $ne
    value: string
    description: Filter by the inventory item's width.
  - key: $in
    value: string,string
    description: Filter by the inventory item's width.
  - key: $nin
    value: string,string
    description: Filter by the inventory item's width.
  - key: $like
    value: string
    description: Filter by the inventory item's width.
  - key: $ilike
    value: string
    description: Filter by the inventory item's width.
  - key: $re
    value: string
    description: Filter by the inventory item's width.
  - key: $contains
    value: string,string
    description: Filter by the inventory item's width.
  - key: $gt
    value: string
    description: Filter by the inventory item's width.
  - key: $gte
    value: string
    description: Filter by the inventory item's width.
  - key: $lt
    value: string
    description: Filter by the inventory item's width.
  - key: $lte
    value: string
    description: Filter by the inventory item's width.
  - key: location_id
    value: string
    description: Filter by the inventory item's associated location IDs.
  - key: with_deleted
    value: "true"
    description: Whether to include deleted records in the result.
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/List Inventory Items.resources/examples
order: 3000
