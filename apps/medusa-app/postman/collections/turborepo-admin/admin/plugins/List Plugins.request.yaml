$kind: http-request
description: Retrieve the list of plugins installed in the Medusa application.
url: "{{baseUrl}}/admin/plugins"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/List Plugins.resources/examples
order: 1000
