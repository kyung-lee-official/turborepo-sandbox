$kind: http-example
request:
  url: "{{baseUrl}}/admin/gift-cards?id=string&customer_id=string&$eq=string&$ne=\
    string&$in=string,string&$nin=string,string&$not=string&$gt=string&$gte=str\
    ing&$lt=string&$lte=string&$like=string&$re=string&$ilike=string&$fulltext=\
    string&$overlap=string,string&$contains=string,string&$contained=string,str\
    ing&$exists=true&limit=3050.1607324519186&offset=3157.9117089204024&order=s\
    tring&fields=string"
  method: GET
  headers:
    - key: Accept
      value: application/json
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    - key: id
      value: string
    - key: customer_id
      value: string
    - key: $eq
      value: string
      description: Filter by a gift card's creation date.
    - key: $ne
      value: string
      description: Filter by a gift card's creation date.
    - key: $in
      value: string,string
      description: Filter by a gift card's creation date.
    - key: $nin
      value: string,string
      description: Filter by a gift card's creation date.
    - key: $not
      value: string
      description: Filter by a gift card's creation date.
    - key: $gt
      value: string
      description: Filter by a gift card's creation date.
    - key: $gte
      value: string
      description: Filter by a gift card's creation date.
    - key: $lt
      value: string
      description: Filter by a gift card's creation date.
    - key: $lte
      value: string
      description: Filter by a gift card's creation date.
    - key: $like
      value: string
      description: Filter by a gift card's creation date.
    - key: $re
      value: string
      description: Filter by a gift card's creation date.
    - key: $ilike
      value: string
      description: Filter by a gift card's creation date.
    - key: $fulltext
      value: string
      description: Filter by a gift card's creation date.
    - key: $overlap
      value: string,string
      description: Filter by a gift card's creation date.
    - key: $contains
      value: string,string
      description: Filter by a gift card's creation date.
    - key: $contained
      value: string,string
      description: Filter by a gift card's creation date.
    - key: $exists
      value: "true"
      description: Filter by a gift card's creation date.
    - key: limit
      value: "3050.1607324519186"
      description: Limit the number of items returned in the list.
    - key: offset
      value: "3157.9117089204024"
      description: The number of items to skip when retrieving a list.
    - key: order
      value: string
      description: The field to sort the data by. By default, the sort order is
        ascending. To change the order to descending, prefix the field name with
        `-`.
    - key: fields
      value: string
      description: Comma-separated fields that should be included in the returned
        data. If a field is prefixed with `+` it will be added to the default
        fields, using `-` will remove it from the default fields. Without prefix
        it will replace the entire default fields.
response:
  statusCode: 404
  statusText: Not Found
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "message": "Entity with id 1 was not found",
        "type": "not_found"
      }
order: 4000
