$kind: http-request
description: Create a gift card.
url: "{{baseUrl}}/admin/gift-cards?fields=string"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
queryParams:
  - key: fields
    value: string
    description: >-
      Comma-separated fields that should be included in the returned data.

      if a field is prefixed with `+` it will be added to the default fields,
      using `-` will remove it from the default fields.

      without prefix it will replace the entire default fields.
body:
  type: json
  content: |-
    {
      "code": "string",
      "value": 20,
      "currency_code": "usd",
      "expires_at": "string",
      "reference_id": "string",
      "reference": "string",
      "line_item_id": "string",
      "customer_id": "string",
      "metadata": {}
    }
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Create Gift Card.resources/examples
order: 3000
