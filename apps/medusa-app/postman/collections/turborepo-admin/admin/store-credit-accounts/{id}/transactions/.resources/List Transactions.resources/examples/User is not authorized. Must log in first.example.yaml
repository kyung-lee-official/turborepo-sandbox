$kind: http-example
request:
  url: "{{baseUrl}}/admin/store-credit-accounts/:id/transactions?id=string&transa\
    ction_group_id=string&$eq=string&$ne=string&$in=string,string&$nin=string,s\
    tring&$not=string&$gt=string&$gte=string&$lt=string&$lte=string&$like=strin\
    g&$re=string&$ilike=string&$fulltext=string&$overlap=string,string&$contain\
    s=string,string&$contained=string,string&$exists=true&limit=3050.1607324519\
    186&offset=3157.9117089204024&order=string&fields=string&q=string"
  method: GET
  headers:
    - key: Accept
      value: text/plain
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    - key: id
      value: string
    - key: transaction_group_id
      value: string
    - key: $eq
      value: string
      description: Filter by a transaction's creation date.
    - key: $ne
      value: string
      description: Filter by a transaction's creation date.
    - key: $in
      value: string,string
      description: Filter by a transaction's creation date.
    - key: $nin
      value: string,string
      description: Filter by a transaction's creation date.
    - key: $not
      value: string
      description: Filter by a transaction's creation date.
    - key: $gt
      value: string
      description: Filter by a transaction's creation date.
    - key: $gte
      value: string
      description: Filter by a transaction's creation date.
    - key: $lt
      value: string
      description: Filter by a transaction's creation date.
    - key: $lte
      value: string
      description: Filter by a transaction's creation date.
    - key: $like
      value: string
      description: Filter by a transaction's creation date.
    - key: $re
      value: string
      description: Filter by a transaction's creation date.
    - key: $ilike
      value: string
      description: Filter by a transaction's creation date.
    - key: $fulltext
      value: string
      description: Filter by a transaction's creation date.
    - key: $overlap
      value: string,string
      description: Filter by a transaction's creation date.
    - key: $contains
      value: string,string
      description: Filter by a transaction's creation date.
    - key: $contained
      value: string,string
      description: Filter by a transaction's creation date.
    - key: $exists
      value: "true"
      description: Filter by a transaction's creation date.
    - key: limit
      value: "3050.1607324519186"
      description: Limit the number of items returned in the list.
    - key: offset
      value: "3157.9117089204024"
      description: The number of items to skip when retrieving a list.
    - key: order
      value: string
      description: The field to sort the data by. By default, the sort order is
        ascending. To change the order to descending, prefix the field name with
        `-`.
    - key: fields
      value: string
      description: Comma-separated fields that should be included in the returned
        data. If a field is prefixed with `+` it will be added to the default
        fields, using `-` will remove it from the default fields. Without prefix
        it will replace the entire default fields.
    - key: q
      value: string
      description: Search query to filter the transactions by their searchable fields.
  pathVariables:
    - key: id
      value: string
      description: The store credit account's ID.
response:
  statusCode: 401
  statusText: Unauthorized
  headers:
    Content-Type: text/plain
  body:
    type: text
    content: Unauthorized
order: 3000
