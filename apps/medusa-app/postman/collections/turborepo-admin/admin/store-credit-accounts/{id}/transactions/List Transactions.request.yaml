$kind: http-request
description: Retrieve a list of transactions in a store credit account. The
  transactions can be filtered by fields like FILTER FIELDS. The transactions
  can also be paginated.
url: "{{baseUrl}}/admin/store-credit-accounts/:id/transactions?id=string&transa\
  ction_group_id=string&$eq=string&$ne=string&$in=string,string&$nin=string,str\
  ing&$not=string&$gt=string&$gte=string&$lt=string&$lte=string&$like=string&$r\
  e=string&$ilike=string&$fulltext=string&$overlap=string,string&$contains=stri\
  ng,string&$contained=string,string&$exists=false&$eq=string&$ne=string&$in=st\
  ring,string&$nin=string,string&$not=string&$gt=string&$gte=string&$lt=string&\
  $lte=string&$like=string&$re=string&$ilike=string&$fulltext=string&$overlap=s\
  tring,string&$contains=string,string&$contained=string,string&$exists=true&li\
  mit=3050.1607324519186&offset=3157.9117089204024&order=string&fields=string&q\
  =string"
method: GET
headers:
  Accept: application/json
queryParams:
  - key: id
    value: string
  - key: transaction_group_id
    value: string
  - key: $eq
    value: string
    description: Filter by a transaction's creation date.
  - key: $ne
    value: string
    description: Filter by a transaction's creation date.
  - key: $in
    value: string,string
    description: Filter by a transaction's creation date.
  - key: $nin
    value: string,string
    description: Filter by a transaction's creation date.
  - key: $not
    value: string
    description: Filter by a transaction's creation date.
  - key: $gt
    value: string
    description: Filter by a transaction's creation date.
  - key: $gte
    value: string
    description: Filter by a transaction's creation date.
  - key: $lt
    value: string
    description: Filter by a transaction's creation date.
  - key: $lte
    value: string
    description: Filter by a transaction's creation date.
  - key: $like
    value: string
    description: Filter by a transaction's creation date.
  - key: $re
    value: string
    description: Filter by a transaction's creation date.
  - key: $ilike
    value: string
    description: Filter by a transaction's creation date.
  - key: $fulltext
    value: string
    description: Filter by a transaction's creation date.
  - key: $overlap
    value: string,string
    description: Filter by a transaction's creation date.
  - key: $contains
    value: string,string
    description: Filter by a transaction's creation date.
  - key: $contained
    value: string,string
    description: Filter by a transaction's creation date.
  - key: $exists
    value: "false"
    description: Filter by a transaction's creation date.
  - key: $eq
    value: string
    description: Filter by a transaction's update date.
  - key: $ne
    value: string
    description: Filter by a transaction's update date.
  - key: $in
    value: string,string
    description: Filter by a transaction's update date.
  - key: $nin
    value: string,string
    description: Filter by a transaction's update date.
  - key: $not
    value: string
    description: Filter by a transaction's update date.
  - key: $gt
    value: string
    description: Filter by a transaction's update date.
  - key: $gte
    value: string
    description: Filter by a transaction's update date.
  - key: $lt
    value: string
    description: Filter by a transaction's update date.
  - key: $lte
    value: string
    description: Filter by a transaction's update date.
  - key: $like
    value: string
    description: Filter by a transaction's update date.
  - key: $re
    value: string
    description: Filter by a transaction's update date.
  - key: $ilike
    value: string
    description: Filter by a transaction's update date.
  - key: $fulltext
    value: string
    description: Filter by a transaction's update date.
  - key: $overlap
    value: string,string
    description: Filter by a transaction's update date.
  - key: $contains
    value: string,string
    description: Filter by a transaction's update date.
  - key: $contained
    value: string,string
    description: Filter by a transaction's update date.
  - key: $exists
    value: "true"
    description: Filter by a transaction's update date.
  - key: limit
    value: "3050.1607324519186"
    description: Limit the number of items returned in the list.
  - key: offset
    value: "3157.9117089204024"
    description: The number of items to skip when retrieving a list.
  - key: order
    value: string
    description: The field to sort the data by. By default, the sort order is
      ascending. To change the order to descending, prefix the field name with
      `-`.
  - key: fields
    value: string
    description: Comma-separated fields that should be included in the returned
      data. If a field is prefixed with `+` it will be added to the default
      fields, using `-` will remove it from the default fields. Without prefix
      it will replace the entire default fields.
  - key: q
    value: string
    description: Search query to filter the transactions by their searchable fields.
pathVariables:
  - key: id
    value: string
    description: The store credit account's ID.
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/List Transactions.resources/examples
order: 1000
