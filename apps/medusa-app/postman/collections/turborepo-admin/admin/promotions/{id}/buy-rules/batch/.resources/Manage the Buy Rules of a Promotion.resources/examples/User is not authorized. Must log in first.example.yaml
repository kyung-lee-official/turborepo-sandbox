$kind: http-example
request:
  url: "{{baseUrl}}/admin/promotions/:id/buy-rules/batch?promotion_type=buyget&ap\
    plication_method_type=percentage&application_method_target_type=items"
  method: POST
  headers:
    - key: Content-Type
      value: application/json
    - key: Accept
      value: text/plain
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    - key: promotion_type
      value: buyget
      description: The promotion's type.
    - key: application_method_type
      value: percentage
      description: The promotion's application method type.
    - key: application_method_target_type
      value: items
      description: The promotion's application method target type.
  pathVariables:
    - key: id
      value: string
      description: The promotion's ID.
  body:
    type: json
    content: |-
      {
        "create": [
          {
            "operator": "gt",
            "attribute": "items.product.id",
            "values": "prod_123",
            "description": "string"
          },
          {
            "operator": "lte",
            "attribute": "items.product.id",
            "values": "prod_123",
            "description": "string"
          }
        ],
        "update": [
          {
            "id": "string",
            "values": "prod_123",
            "operator": "gt",
            "description": "string",
            "attribute": "items.product.id"
          },
          {
            "id": "string",
            "values": "prod_123",
            "operator": "in",
            "description": "string",
            "attribute": "items.product.id"
          }
        ],
        "delete": [
          "string",
          "string"
        ]
      }
response:
  statusCode: 401
  statusText: Unauthorized
  headers:
    Content-Type: text/plain
  body:
    type: text
    content: Unauthorized
order: 3000
