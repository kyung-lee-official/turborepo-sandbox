$kind: http-example
request:
  url: "{{baseUrl}}/store/collections?fields=string&offset=5161.580294528132&limi\
    t=7575.827557165484&order=string&title=string&$eq=string&$ne=string&$in=str\
    ing,string&$nin=string,string&$not=string&$gt=string&$gte=string&$lt=string\
    &$lte=string&$like=string&$re=string&$ilike=string&$fulltext=string&$overla\
    p=string,string&$contains=string,string&$contained=string,string&$exists=tr\
    ue&handle=string&q=string&with_deleted=true&id=string&locale=en-US"
  method: GET
  headers:
    - key: x-publishable-api-key
      value: string
      description: Publishable API Key created in the Medusa Admin.
    - key: x-medusa-locale
      value: en-US
      description: The locale in BCP 47 format to retrieve localized content.
    - key: Accept
      value: application/json
  queryParams:
    - key: fields
      value: string
      description: Comma-separated fields that should be included in the returned
        data. if a field is prefixed with `+` it will be added to the default
        fields, using `-` will remove it from the default fields. without prefix
        it will replace the entire default fields.
    - key: offset
      value: "5161.580294528132"
      description: The number of items to skip when retrieving a list.
    - key: limit
      value: "7575.827557165484"
      description: Limit the number of items returned in the list.
    - key: order
      value: string
      description: The field to sort the data by. By default, the sort order is
        ascending. To change the order to descending, prefix the field name with
        `-`.
    - key: title
      value: string
    - key: $eq
      value: string
      description: Filter by the collection's creation date.
    - key: $ne
      value: string
      description: Filter by the collection's creation date.
    - key: $in
      value: string,string
      description: Filter by the collection's creation date.
    - key: $nin
      value: string,string
      description: Filter by the collection's creation date.
    - key: $not
      value: string
      description: Filter by the collection's creation date.
    - key: $gt
      value: string
      description: Filter by the collection's creation date.
    - key: $gte
      value: string
      description: Filter by the collection's creation date.
    - key: $lt
      value: string
      description: Filter by the collection's creation date.
    - key: $lte
      value: string
      description: Filter by the collection's creation date.
    - key: $like
      value: string
      description: Filter by the collection's creation date.
    - key: $re
      value: string
      description: Filter by the collection's creation date.
    - key: $ilike
      value: string
      description: Filter by the collection's creation date.
    - key: $fulltext
      value: string
      description: Filter by the collection's creation date.
    - key: $overlap
      value: string,string
      description: Filter by the collection's creation date.
    - key: $contains
      value: string,string
      description: Filter by the collection's creation date.
    - key: $contained
      value: string,string
      description: Filter by the collection's creation date.
    - key: $exists
      value: "true"
      description: Filter by the collection's creation date.
    - key: handle
      value: string
    - key: q
      value: string
      description: Search term to filter the collection's searchable properties.
    - key: with_deleted
      value: "true"
      description: Whether to include deleted records in the result.
    - key: id
      value: string
    - key: locale
      value: en-US
      description: The locale in BCP 47 format to retrieve localized content.
response:
  statusCode: 404
  statusText: Not Found
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "message": "Entity with id 1 was not found",
        "type": "not_found"
      }
order: 4000
