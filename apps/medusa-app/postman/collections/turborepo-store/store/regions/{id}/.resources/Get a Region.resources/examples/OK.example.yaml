$kind: http-example
request:
  url: "{{baseUrl}}/store/regions/:id?fields=string&locale=en-US"
  method: GET
  headers:
    - key: x-publishable-api-key
      value: string
      description: Publishable API Key created in the Medusa Admin.
    - key: x-medusa-locale
      value: en-US
      description: The locale in BCP 47 format to retrieve localized content.
    - key: Accept
      value: application/json
  queryParams:
    - key: fields
      value: string
      description: Comma-separated fields that should be included in the returned
        data. if a field is prefixed with `+` it will be added to the default
        fields, using `-` will remove it from the default fields. without prefix
        it will replace the entire default fields.
    - key: locale
      value: en-US
      description: The locale in BCP 47 format to retrieve localized content.
  pathVariables:
    - key: id
      value: string
      description: The region's ID.
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "region": {
          "id": "string",
          "name": "string",
          "currency_code": "usd",
          "automatic_taxes": false,
          "countries": [
            {
              "id": "string",
              "iso_2": "us",
              "iso_3": "usa",
              "num_code": "string",
              "name": "string",
              "display_name": "string"
            },
            {
              "id": "string",
              "iso_2": "us",
              "iso_3": "usa",
              "num_code": "string",
              "name": "string",
              "display_name": "string"
            }
          ],
          "payment_providers": [
            {
              "is_enabled": true,
              "id": "string"
            },
            {
              "is_enabled": true,
              "id": "string"
            }
          ],
          "metadata": {},
          "created_at": "1987-06-13T05:27:45.691Z",
          "updated_at": "2018-08-24T20:52:16.051Z"
        }
      }
order: 1000
