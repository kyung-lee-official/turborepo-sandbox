$kind: http-request
description: >-
  # Deprecated


  This route has been disabled for security reasons as it exposes tokens in the
  response body. Use 'POST /auth/sign-in/:actor_type/:auth_provider' instead.


  ~~Authenticate a customer and receive the JWT token to be used in the header
  of subsequent requests.~~


  ~~When used with a third-party provider, such as Google, the request returns
  a~~ `~~location~~` ~~property. You redirect to the specified URL in your
  storefront to continue authentication with the third-party service.~~
url: "{{baseUrl}}/auth/customer/:auth_provider"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
pathVariables:
  - key: auth_provider
    value: emailpass
    description: The provider used for authentication.
body:
  type: json
  content: "{}"
examples: ./.resources/Authenticate Customer (Deprecated).resources/examples
order: 6000
